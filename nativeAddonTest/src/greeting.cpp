#include <iostream>
#include <string>
#include "greeting.h"
using namespace std;

std::string helloUser( std::string name ) {
  return "Hello " + name + "!";
}

void moveBot(vector<int> state) {
  // 
}
